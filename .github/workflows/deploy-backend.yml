name: Deploy Backend

on:
  push:
    branches: [ "main" ]   # listens on main branch
  workflow_dispatch:        # allows manual run

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run backend deploy script
        run: bash /opt/deploy-backend.sh

